"""Technical Analysis paper-trading engine.

Standalone Python program (Claude is NOT the runtime). Pure-Python, no DB/SQL;
all state lives in JSON/CSV. Paper trading only — never places real orders.
"""
__version__ = "0.1.0"
